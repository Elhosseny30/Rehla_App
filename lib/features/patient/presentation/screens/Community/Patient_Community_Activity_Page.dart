import 'package:flutter/material.dart';

class PatientCommunityActivityPage extends StatelessWidget {
  const PatientCommunityActivityPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("activity"),
    );
  }
}
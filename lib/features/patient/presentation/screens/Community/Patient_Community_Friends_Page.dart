import 'package:flutter/material.dart';

class PatientCommunityFriendsPage extends StatelessWidget {
  const PatientCommunityFriendsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("friends"),
    );
  }
}
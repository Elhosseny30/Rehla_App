import 'package:flutter/material.dart';

class PatientCommunityProfilePage extends StatelessWidget {
  const PatientCommunityProfilePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("profile"),
    );
  }
}